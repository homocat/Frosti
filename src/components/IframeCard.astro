---

---
<script is:inline>

    const frame = document.getElementById('frame')

    const fdoc = frame.contentDocument || frame.contentWindow.document
    // console.log(frame)

    frame.onload = () => {
        const board = frame.getElementsByClassName('bg-base-100 shadow-xl')
        // console.log(board)
        // console.log(`frame.contentWindow.test:`, frame.contentWindow.test);

        // frame.contentWindow.test()

    }
</script>
<iframe id="frame" class="back-card card bg-base-100 shadow-xl" style="width: 90%; height: 85vh;" src="https://anitabi.cn/map" frameborder="0"></iframe>
